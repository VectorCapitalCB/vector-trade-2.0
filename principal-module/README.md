git pull##valida si hay dependencias vulnerables (mantener actualizada la dependencia que valida 8.3.1)
mvn dependency-check:check

gh auth switch

gh auth switch VectorCapitalCB
git config user.name "VectorCapitalCB"
git config user.email "support@vectorcapital.cl"